#include <iostream>

enum Currencies {
    BTC = 0,
    ETH = 1
};

enum Actions {
    BUY = 0,
    SELL = 1,
    WAIT = 2
};

struct toBeReturned {
    Currencies currency;
    Actions action;
};

toBeReturned botBrain(float price, const char* ) {
    toBeReturned result;
    
    result.currency
    
    return result
}

int main() {
  std::cout << "Hello World!\n";
  
  toBeReturned LeL = {.currency = BTC, .action = WAIT};
}